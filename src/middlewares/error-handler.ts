import { Request, Response } from 'express';
import { StatusCodes } from 'http-status-codes';
const errorHandlerMiddleware = async (req: Request, res: Response) => {};

export default errorHandlerMiddleware;
